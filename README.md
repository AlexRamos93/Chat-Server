# Chat-Server
Server written in C++ that echos the client's message. 
It allows multiple clients to connect using sockets, the fd_set structure, the select() function and three macros: FD_CLR, FD_SET and FD_ZERO.
